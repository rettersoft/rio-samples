init: index.init
getState: index.getState
getInstanceId: index.getInstanceId
methods:
  - method: sendMailToSubscribers
    type: STATIC
    handler: sendMail.sendMailToSubscribers
    schedule: cron(0 18 ? * MON-FRI *) # Monday to Friday every day 18.00
